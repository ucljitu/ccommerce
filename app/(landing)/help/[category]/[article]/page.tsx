import type { Metadata } from "next";
import Link from "next/link";
import { ArrowLeft, ArrowRight, CheckCircle2, Home, Info, Mail, MessageCircle } from "lucide-react";
import { getHelpArticle, helpArticleHref, publishedCategories, support } from "@/lib/help-content";
import { notFound } from "next/navigation";

type Props = { params: Promise<{ category: string; article: string }> };

export function generateStaticParams() {
  return publishedCategories.flatMap((category) => category.articles.map((article) => ({ category: category.slug, article: article.slug })));
}

export async function generateMetadata({ params }: Props): Promise<Metadata> {
  const values = await params;
  const result = getHelpArticle(values.category, values.article);
  if (!result) return { title: "Help Article Not Found", robots: { index: false, follow: false } };
  return { title: `${result.article.title} | C Commerce Help`, description: result.article.summary, alternates: { canonical: helpArticleHref(result.category.slug, result.article.slug) } };
}

export default async function HelpArticlePage({ params }: Props) {
  const values = await params;
  const result = getHelpArticle(values.category, values.article);
  if (!result) notFound();
  const { category, article } = result;
  const index = category.articles.findIndex((item) => item.slug === article.slug);
  const previous = category.articles[index - 1];
  const next = category.articles[index + 1];
  const related = category.articles.filter((item) => item.slug !== article.slug).slice(0, 3);
  const whatsappUrl = `https://wa.me/${support.whatsappNumber}?text=${encodeURIComponent(`${support.whatsappMessage} Article: ${article.title}`)}`;

  return <div className="min-h-screen bg-slate-50 pt-24 pb-16"><main className="mx-auto max-w-6xl px-4 sm:px-6"><nav aria-label="Breadcrumb"><ol className="flex flex-wrap items-center gap-2 text-sm text-slate-500"><li><Link href="/help" className="inline-flex items-center gap-1 hover:text-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500"><Home className="h-4 w-4" />Help Center</Link></li><li aria-hidden="true">/</li><li><Link href={`/help/${category.slug}`} className="hover:text-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500">{category.title}</Link></li><li aria-hidden="true">/</li><li aria-current="page" className="max-w-full truncate text-slate-800">{article.title}</li></ol></nav><div className="mt-8 grid gap-8 lg:grid-cols-[minmax(0,780px)_280px] lg:items-start"><article className="rounded-3xl border border-slate-200 bg-white p-6 sm:p-10"><span className="inline-flex rounded-full bg-blue-50 px-3 py-1 text-xs font-semibold text-blue-700">{category.title}</span><h1 className="mt-4 text-3xl font-bold tracking-tight text-slate-950 sm:text-4xl">{article.title}</h1><p className="mt-4 text-lg leading-8 text-slate-600">{article.summary}</p><p className="mt-3 text-sm text-slate-400">{article.readingTime} min read</p><hr className="my-8 border-slate-100" /><section><h2 className="text-xl font-bold text-slate-900">Overview</h2><p className="mt-3 leading-7 text-slate-600">{article.summary} Follow the steps below in your merchant dashboard.</p></section>{article.beforeYouStart?.length ? <section className="mt-8"><h2 className="text-xl font-bold text-slate-900">Before You Start</h2><ul className="mt-4 space-y-2">{article.beforeYouStart.map((item) => <li key={item} className="flex gap-3 text-slate-600"><CheckCircle2 className="mt-1 h-5 w-5 shrink-0 text-blue-600" />{item}</li>)}</ul></section> : null}<section className="mt-8"><h2 className="text-xl font-bold text-slate-900">Step-by-Step Instructions</h2><ol className="mt-5 space-y-5">{article.steps.map((step, stepIndex) => <li key={step.title} className="flex gap-4"><span className="flex h-9 w-9 shrink-0 items-center justify-center rounded-full bg-blue-600 text-sm font-bold text-white">{stepIndex + 1}</span><div><h3 className="font-semibold text-slate-900">{step.title}</h3><p className="mt-1 leading-7 text-slate-600">{step.detail}</p></div></li>)}</ol></section>{article.notes?.length ? <section className="mt-8 rounded-2xl border border-blue-100 bg-blue-50 p-5"><h2 className="flex items-center gap-2 font-bold text-blue-950"><Info className="h-5 w-5" />Important Notes</h2><ul className="mt-3 space-y-2 text-sm leading-6 text-blue-900">{article.notes.map((note) => <li key={note}>{note}</li>)}</ul></section> : null}{article.problems?.length ? <section className="mt-8"><h2 className="text-xl font-bold text-slate-900">Common Problems</h2>{article.problems.map((item) => <div key={item.problem} className="mt-4"><h3 className="font-semibold text-slate-900">{item.problem}</h3><p className="mt-1 text-slate-600">{item.solution}</p></div>)}</section> : null}<section className="mt-9 border-t border-slate-100 pt-8"><h2 className="text-xl font-bold text-slate-900">Related Articles</h2><ul className="mt-4 space-y-3">{related.map((item) => <li key={item.slug}><Link href={helpArticleHref(category.slug, item.slug)} className="inline-flex items-center gap-2 text-blue-700 hover:underline focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500">{item.title}<ArrowRight className="h-4 w-4" /></Link></li>)}</ul></section></article><aside className="space-y-5 lg:sticky lg:top-24"><div className="rounded-2xl border border-slate-200 bg-white p-5"><h2 className="font-bold text-slate-900">Still need help?</h2><p className="mt-2 text-sm leading-6 text-slate-500">{support.availability}</p><div className="mt-4 space-y-2"><Link href="/contact" className="flex min-h-11 items-center gap-2 rounded-xl bg-blue-600 px-4 py-2 text-sm font-semibold text-white hover:bg-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2"><MessageCircle className="h-4 w-4" />Contact Support</Link><a href={whatsappUrl} target="_blank" rel="noopener noreferrer" className="flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 hover:text-emerald-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500"><MessageCircle className="h-4 w-4" />WhatsApp Support</a><a href={`mailto:${support.email}`} className="flex min-h-11 items-center gap-2 rounded-xl border border-slate-200 px-4 py-2 text-sm font-semibold text-slate-700 hover:text-blue-700 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500"><Mail className="h-4 w-4" />Email Support</a></div></div></aside></div><nav aria-label="Article navigation" className="mt-8 grid gap-3 sm:grid-cols-2">{previous ? <Link href={helpArticleHref(category.slug, previous.slug)} className="rounded-2xl border border-slate-200 bg-white p-5 hover:border-blue-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500"><span className="text-xs text-slate-400">Previous article</span><span className="mt-1 flex items-center gap-2 font-semibold text-slate-800"><ArrowLeft className="h-4 w-4" />{previous.title}</span></Link> : <span />}{next ? <Link href={helpArticleHref(category.slug, next.slug)} className="rounded-2xl border border-slate-200 bg-white p-5 text-right hover:border-blue-200 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500"><span className="text-xs text-slate-400">Next article</span><span className="mt-1 flex items-center justify-end gap-2 font-semibold text-slate-800">{next.title}<ArrowRight className="h-4 w-4" /></span></Link> : null}</nav></main></div>;
}
